# Claude Flow runtime files
data/
logs/
sessions/
neural/
*.log
*.tmp

# Daemon-Laufzeit-Zustand (fehlt im ruflo-init-Template): PID-Datei,
# Worker-State und Metrik-Snapshots, die der ruflo-Daemon zur Laufzeit
# schreibt — maschinenspezifisch, nicht versionieren.
daemon.pid
daemon-state.json
metrics/
learning/
security/
